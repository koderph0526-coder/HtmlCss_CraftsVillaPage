// Fix switch case or if/else to make submit button inactive until valid email is added
const emailInput = document.querySelector(".emailSubmit");
const submitBtn = document.querySelector(".btnSubmit");

//Also add a connection between the checkboxes tot heir corresponding options!
//Might be simpler to create the checkboxes here and append them to their respective options?

// Add  a function to alter nav a bit(making it smaller) and have it export to the other js files that is to be attached to the childPages?
